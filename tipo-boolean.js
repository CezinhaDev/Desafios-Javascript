const estudante =  'cesar';
const aprovado = true;
const nota = 7.5;
const media = 6.0;
const mensagem = `O estudante ${estudante} foi ${aprovado ? 'aprovado' : 'reprovado'} com nota ${nota}. A média da turma é ${media}.`;
console.log(mensagem);